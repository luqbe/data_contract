resources:
  jobs:
    data_generator:
      name: data_generator
      tasks:
        - task_key: data_gen
          notebook_task:
            notebook_path: /Workspace/data_contract_demo/generator.py
            source: WORKSPACE
      queue:
        enabled: true
      performance_target: PERFORMANCE_OPTIMIZED
