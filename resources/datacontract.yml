id: urn:orders-contract
version: 1.0.0
name: orders_demo
description: "Data contract for the orders table"

# --- ADD THIS SECTION ---
servers:
  production:
    type: databricks
    endpoint: "/sql/1.0/warehouses/a87b6d986e3818f7"
    catalog: main
    schema: default
# ------------------------

schema:
  columns:
    order_id:
      type: string
      required: true
    total_amount:
      type: number
      required: true
    customer_email:
      type: string
      format: email